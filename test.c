'\n    * 这是你要修改的代码。\n    @CODE:\n#include <stdio.h>\n\nvoid bubble_sort(int arr[], int n) {\n    for (int i = 0; i < n - 1; i++) {\n        for (int j = 0; j < n - i - 1; j++) {\n            if (arr[j] > arr[j + 1]) {\n                int temp = arr[j];\n                arr[j] = arr[j + 1];\n                arr[j + 1] = temp;\n            }\n        }\n    }\n}\n\nint main() {\n    int arr[] = {64, 34, 25, 12, 22, 11, 90}; // 原始代码中已经给出了数组定义，并且包含了边界限定，无需修改\n    int n = sizeof(arr) / sizeof(arr[0]);\n\n    bubble_sort(arr, n);\n\n    printf("Sorted array is:\\n");\n    for (int i = 0; i < n; i++) {\n        printf("%d ", arr[i]);\n    }\n    printf("\\n");\n\n    return 0;\n}\n'